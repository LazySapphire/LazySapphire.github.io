# Note Publishing - 2026-W23

## 2026-06-03 - 新增论文阅读笔记栏目并发布 Heracles

背景:

- 需要将 `.tmp/papers_read/heracles_2603_27756` 中的本地论文阅读笔记发布到网站，并建立后续论文笔记发布流程。

实施内容:

- 新增 `content/papers/` 论文阅读笔记栏目。
- 发布 `content/papers/heracles-2603-27756/` page bundle。
- 复制整理后的 6 张正文图片，未发布 PDF、TXT 和 raw extracted artifacts。
- 启用 `math: true`，配置 KaTeX 和 Goldmark passthrough。
- 新增 `.ai/skills/paper-note-publishing/` 并更新 `.ai/AGENTS.md`。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- paper-note publishing skill 校验通过。
- 生成 HTML 包含 KaTeX、图片资源且无 `.tmp` 或 raw artifact 引用。

## 2026-06-03 - 发布 PARC 论文阅读笔记

背景:

- 将 `.tmp/papers_read/parc_2025` 中的本地 Markdown 论文阅读笔记整理发布为第二篇公开论文笔记。

实施内容:

- 新增 `content/papers/parc-2025/` page bundle。
- 设置 `math: true` 和 `cover: "fig2_parc_loop.png"`。
- 复制 11 张精选 `fig*.png`。
- 移除本地 PDF、TXT、raw image/page 引用。
- 将主要公式块转为 display math，将变量和模型表达式转为 inline LaTeX。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- paper-note publishing skill 校验通过。
- 生成页面包含 KaTeX、正文图片和列表缩略图。

## 2026-06-04 - 标准化论文列表元数据

背景:

- 论文阅读笔记列表需要更像索引页，标题应短而可扫读，简介应说明论文核心内容。

实施内容:

- 列表模板优先使用 `list_title` 渲染卡片标题和封面 `alt`。
- 为 Heracles 与 PARC 增加 `list_title`。
- 将 Heracles 与 PARC 的 `description` 改为一句话论文介绍。
- 更新 paper-note publishing skill，区分 `title`、`list_title`、`description` 和 `paper_title`。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- paper-note publishing skill 校验通过。

## 2026-06-04 - 发布 MuGen 论文阅读笔记

背景:

- 新增 MUGEN 论文阅读笔记已整理到 `content/papers/mugen-2605-24592/`。

实施内容:

- 发布 `content/papers/mugen-2605-24592/` page bundle。
- 设置 `list_title: "MuGen"` 和一句话 `description`。
- 保留 6 张 curated figures，并使用 `fig2_system_overview.png` 作为列表封面。
- 为 `.prose` 表格和代码块补充横向滚动样式，适配宽表格。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- 线上检查 `/papers/` 与 `/papers/mugen-2605-24592/` 通过。

## 2026-06-05 - 发布阶梯数据重建技术报告

背景:

- 新增本地随笔 `.tmp/somenotes/report_2026_06_05_stairs_reconstruction_method.md`，需要发布到新 `notes` 栏目。

实施内容:

- 发布 `content/notes/stairs-reconstruction-method/` page bundle。
- 添加 `math: true`、`list_title`、一句话 `description`、tags 和 categories。
- 保留源笔记中的状态与方向元数据。
- 将 display math 从 `\[...\]` 统一转换为 `$$...$$`，避免 Markdown 将公式块误解析为标题。
- 将公式块中独立成行的 `=` / `-` 改为 LaTeX 等价写法 `{=}` / `{-}`，阻断 setext heading 解析。
- 保留 Heracles 与 PARC 当前工作区笔记改动，并纳入同次提交。

验证:

- Hugo 构建与生产产物检查通过后提交。
