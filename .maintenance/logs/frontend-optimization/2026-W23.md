# Frontend Optimization - 2026-W23

## 2026-06-03 - 优化论文笔记阅读体验

背景:

- Heracles 论文笔记上线后，inline 变量、论文列表封面和桌面正文宽度需要改进。

实施内容:

- 将正文数学变量和紧凑表达式改为 inline LaTeX。
- 增加 `cover` front matter，并让列表模板显示代表图缩略图。
- 使用 Hugo image pipeline 生成 `360x225` 缩略图。
- 放宽论文正文阅读宽度，并保持内容区略偏左。
- 更新 paper-note publishing skill 中的 inline math、cover 和列表图规则。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- paper-note publishing skill 校验通过。
- 生成的论文列表和 Heracles 页面包含预期缩略图、数学分隔符和 KaTeX 资源。

## 2026-06-04 - 优化主页导航与语言主题控件

背景:

- 顶部导航和首页入口需要更贴合个人项目、履历与阅读笔记网站的定位。

实施内容:

- 从顶部导航和首页首屏入口移除 `Blog`。
- 将 `Papers` 导航入口改为 `Reading Notes`，中文 UI 为 `阅读笔记`。
- 将 `Theme` 文本按钮改为太阳/月亮图标按钮。
- 增加 `中/英` 语言切换，仅翻译显式标记的导航、首页入口、栏目卡片和页脚文案。
- 将 `.tmp/芙莉莲.png` 复制为 `static/img/frieren.png`，作为白天模式主页头像。
- 为 `GitHub` 等外链增加跳转图标和下划线提示。

验证:

- Hugo 构建通过。
- 生产产物检查通过。
- 线上检查首页旧 `Blog` / `Theme` / `Papers` 入口不存在。

