# Frontend Optimization - 2026-W34

## 2026-08-19 - 细分论文阅读主题并支持双击打开

背景:

- 论文阅读页左侧原先直接展示所有细粒度 `tags`，模型、方法、任务和数据集名称混在同一个标签云中。
- 论文条目需要先单击选中，再通过右侧“打开条目”按钮进入详情页。

实施内容:

- 为论文与论文速读条目统一补充稳定的 `topics` 字段。
- 将论文阅读页左侧筛选入口调整为主题筛选，覆盖 `WAM`、`Humanoid`、`Robotic`、`Pianist`、`Dataset`、`Motion` 和 `Physics`。
- 在详情面板中分开展示主题和细分标签；细分标签继续参与搜索，不做破坏性删除。
- 为论文表格行增加双击跳转，保留单击选择、键盘操作和“打开条目”按钮。

验证:

- `git diff --check`
- `.tmp/hugo-bin/usr/local/bin/hugo --minify --gc --cleanDestinationDir`
- `scripts/check-production-artifacts.sh public`
- 本地浏览器检查论文阅读页主题筛选与双击导航。
