# 新增论文阅读笔记栏目

日期: 2026-06-03

## 背景

本次维护需要把 `.tmp/papers_read/heracles_2603_27756` 中的本地论文阅读笔记发布到个人静态网站，并为后续持续发布论文笔记建立可复用流程。

## 实施内容

- 新增 `content/papers/` 论文阅读笔记栏目，并在主导航和首页 section 入口中加入 Papers。
- 将 Heracles 阅读笔记发布为 Hugo page bundle: `content/papers/heracles-2603-27756/`。
- 仅复制整理后的 6 张正文图片，未发布本地 PDF、TXT、raw extracted 图片等临时资料。
- 为论文页启用 `math: true`，并在页面 head 中按需加载 KaTeX。
- 开启 Hugo Goldmark passthrough，避免 Markdown 渲染阶段破坏 LaTeX 反斜杠。
- 为论文笔记页面增加专用 CSS，使图片保持阅读宽度，公式可横向滚动。
- 新增 `.ai/skills/paper-note-publishing/`，记录未来处理本地论文 Markdown 笔记的流程。
- 更新 `.ai/AGENTS.md`，把 Paper Note Publishing 加入 skill 选择索引。

## 关键决策

- 论文笔记内容作为站点源码提交，来源于 `.tmp/` 的本地草稿不提交。
- 图片采用 page bundle 相对路径，便于每篇论文独立维护。
- 公式使用 `$...$`、`$$...$$`、`\(...\)`、`\[...\]` 分隔符，并由 KaTeX auto-render 在浏览器端渲染。
- `public/` 仍视为 Hugo 构建产物，不作为仓库源码提交。

## 验证

- `.tmp/hugo-extract/usr/local/bin/hugo --minify --gc`
- `scripts/check-production-artifacts.sh public`
- `python3 /home/hypnos/.codex/skills/.system/skill-creator/scripts/quick_validate.py .ai/skills/paper-note-publishing`
- 检查生成的 `public/papers/heracles-2603-27756/index.html`，确认包含 KaTeX 资源、`paper-note` 类、6 张 page bundle 图片，且未残留 `.tmp/` 或 `raw_extracted` 引用。
- 检查生成 HTML 中的 LaTeX 源码，确认 `\\` 和 `\left\|...\right\|` 等反斜杠内容被保留。

## 后续

- 后续新增论文笔记时优先读取 `.ai/skills/paper-note-publishing/SKILL.md`。
- 如果未来希望完全离线访问公式渲染，可考虑将 KaTeX 资源 vendored 到 `static/` 或通过构建流程打包。
