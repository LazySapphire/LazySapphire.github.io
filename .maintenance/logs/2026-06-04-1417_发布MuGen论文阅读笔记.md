# 发布 MuGen 论文阅读笔记

## 背景

新增论文阅读笔记已整理到 `content/papers/mugen-2605-24592/`，需要纳入 Hugo 站点并符合当前论文卡片元数据规则。

## 实施内容

- 发布 `content/papers/mugen-2605-24592/` 页面 bundle。
- 增加 `list_title: "MuGen"`，用于论文列表卡片短标题。
- 将 `description` 改为一句话论文介绍。
- 保留 `math: true`，让正文公式继续由 KaTeX 渲染。
- 保留 6 张 curated figures，并使用 `fig2_system_overview.png` 作为列表封面。
- 为 `.prose` 表格和代码块补充横向滚动样式，适配 MUGEN 笔记中的宽表格。

## 决策

- 单页标题继续使用 `MuGen 论文阅读笔记`。
- 列表页标题使用论文简称 `MuGen`。
- 不发布 PDF、TXT 或 `.tmp/` 临时资源。

## 验证

```bash
.tmp/hugo-extract/usr/local/bin/hugo --minify --gc
scripts/check-production-artifacts.sh public
```

额外检查了生成的 `/papers/` 列表和 `/papers/mugen-2605-24592/` 页面，确认卡片标题、简介、KaTeX 资源和图片引用正常。

