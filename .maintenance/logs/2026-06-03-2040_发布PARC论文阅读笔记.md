# 发布 PARC 论文阅读笔记

日期: 2026-06-03

## 背景

本次维护将 `.tmp/papers_read/parc_2025` 中的本地 Markdown 论文阅读笔记整理并发布到 Papers 栏目，作为第二篇公开论文笔记。

## 实施内容

- 新增 Hugo page bundle: `content/papers/parc-2025/`。
- 从本地 README 导入正文，并添加 Hugo front matter。
- 设置 `math: true` 和 `cover: "fig2_parc_loop.png"`。
- 复制 11 张精选 `fig*.png` 到 page bundle。
- 未发布本地 `PARC_2025.pdf`、`PARC_2025.txt`、`raw_images/`、`raw_pages/`。
- 将正文图片路径从 `assets/fig*.png` 改为 page bundle 相对路径。
- 移除本地材料和 raw asset 说明。
- 将主要公式块转为 `$$...$$` display math。
- 将变量、loss、维度和模型调用等 code span 转为 inline LaTeX。
- 保留流程说明类 pseudo-code 代码块，避免过度数学化。

## 关键决策

- 使用 `parc-2025` 作为稳定 slug。
- 选择 `fig2_parc_loop.png` 作为列表 cover，因为它最直接表达 PARC 的 generator/tracker 数据闭环。
- 公式续行使用 `aligned` 环境，避免 Markdown 将以 `+` 开头的 LaTeX 续行解析成列表项。
- `no physics correction` 属于实验标签，保留为 code span 而不是数学变量。

## 验证

- `.tmp/hugo-extract/usr/local/bin/hugo --minify --gc`
- `scripts/check-production-artifacts.sh public`
- `python3 /home/hypnos/.codex/skills/.system/skill-creator/scripts/quick_validate.py .ai/skills/paper-note-publishing`
- 检查 `public/papers/index.html`，确认 PARC 列表卡片包含 `list-thumb` 和生成缩略图。
- 检查 `public/papers/parc-2025/index.html`，确认 KaTeX 资源、`paper-note` 类、正文图片和公式分隔符存在。
- 检查发布内容未残留 `.tmp`、`raw_images`、`raw_pages`、本地 PDF/TXT 引用。

## 后续

- 如后续需要更强的公式一致性，可考虑给 paper-note publishing skill 增加一个公式规范化脚本。
- 如论文图片数量继续增加，可考虑为正文图片也生成响应式尺寸。
